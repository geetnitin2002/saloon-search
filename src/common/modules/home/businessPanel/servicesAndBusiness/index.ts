/**
 * @description Entry point for the Home module list.
 */
import view from "./serviceAndBusinessSearchContainer";

// module name
export const moduleName = "Search";

export default {
  name: moduleName,
  view
};
