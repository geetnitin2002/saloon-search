/**
 * @description Entry point for the reset password flow.
 */
import view from "./resetPassword";

// module name
export const moduleName = "resetpassword";

export default {
  name: moduleName,
  view
};
